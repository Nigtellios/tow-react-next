/* This file is made by purpose, I didn't wanted to overload project with any other libraries, that's why I didn't use dotenv package. */

const config = {
  url: 'http://localhost:8888',
  graphqlUrl: 'http://localhost:8888/graphql',
};

export default config;
